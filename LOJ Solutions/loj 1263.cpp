#inclde<bits/stdc++.h>
using namespace std;

const int N=1005;
int ara[N];
vector<int> graph[N];

int main(int argc,char const *atgv[]) {
    int t,tc;
    scanf("%d",&t);
    for(tc=1;tc<=t;tc++) {
        int n,q;
        scanf("%d%d",&n,&q);
        for(int i=1;i<=n;i++) {
            scanf("%d",&ara[i]);
        }
        for(int i=1;i<=q;i++) {
            int u,v;
            scanf("%d%d",&u,&v);
            graph[u].pish_()
        }
    }
    return 0;
}
