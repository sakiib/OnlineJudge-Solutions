#include<bits/stdc++.h>
using namespace std;
map<int,int>mp;
vector<int>mv3;
vector<int>mv2;
int main()
{
    int n=5,num,sum=0,ara[10001],cnt=0;
    for(int i=0;i<n;i++){
        cin>>ara[i];
        sum+=ara[i];
    }
    sort(ara,ara+n);

    return 0;
}
