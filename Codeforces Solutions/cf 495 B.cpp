#include <bits/stdc++.h>
using namespace std;

int n , m;

int main( int argc, char const *argv[] ) {
        cin >> n >> m;
        for( int i = 1; i <= m; i++ ) {
                int l , r;
                cin >> l >> r;
        }
        for( int i = 1; i <= n; i++ ) cout << i%2 ;
        return 0;
}
