#include <bits/stdc++.h>
using namespace std;

map<char,bool> M;

int main( int argc, char const *argv[] ) {
    int k;
    cin >> k;
    string str;
    cin >> str;
    for(int i = 0; i < k; i++ ) {
        M[ 'a'+i ] = true;
    }

    for( int i = 0; i <
    return 0;
}
