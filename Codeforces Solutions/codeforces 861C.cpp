#include<bits/stdc++.h>
using namespace std;

int main(int argc,char const *argv[]) {
    string str;
    cin>>str;
    for(int i=0;i<str.size();i++) {
        if(cons(str[i])&&cons(str[i+1])&&cons(str[i+2])) {

        }
    }
    return 0;
}
