#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin>>t;
    getchar();
    while(t--) {
        string str;
        int cnt=0;
        getline(cin,str);
        for(int i=0;i<str.size();i++) {
            if(str[i]==' ') cnt++;
        }
        if(cnt==0) {

        }
    }
}
